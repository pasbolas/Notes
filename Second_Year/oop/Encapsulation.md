

### String Gatekeepers 
1. .trim()
2. .isEmpty()
3. .length()
4. .toUpperCase()
5. .matches(String regex)
6. .charAt(int index) // returns the index where the first char is
7. .indexOf(char ) // 
8. .contains(string str)
9. .substring(int start, int end)
10. .replaceAll(string regex, string replacement)
11. .split(string regex)

### Int Gatekeepers

so these are just if else inside the setfunction, like
1. range
2. non-negative
3. upper bound
4. even/odd